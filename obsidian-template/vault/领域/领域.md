# 领域
```dataviewjs
const {Files,WaitLoading} = await cJS();
await WaitLoading.wait();
let notes = Files.getChildNotes(dv.current().file.folder);
dv.table([""], notes.map(f => [dv.page(f).file.link]));
```
